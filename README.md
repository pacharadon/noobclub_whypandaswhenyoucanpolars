# noobclub — why pandas when you can Polars

Polars Playground: a 20-lesson gated course on Polars — build, filter, aggregate, join and window over commerce, ride and farm datasets. Pure-Polars main path with pandas spoilers on demand.

Single-file app (`index.html`), no build step — React 18 + Tailwind via CDN.

**Live:** https://pacharadon.github.io/polars-playground/
